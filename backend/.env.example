# Backend env — copy to .env (gitignored)
# Production MUST override JWT_SECRET with a strong random string.

PORT=4000
JWT_SECRET=dev-secret-change-me
DB_PATH=./data/eventplanner.db
COMPANY_NAME=Event Planner Pro
CURRENCY=Rs.
